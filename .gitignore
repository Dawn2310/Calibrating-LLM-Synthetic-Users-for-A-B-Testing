# ── Environments ──
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.so

# ── Jupyter Notebook ──
.ipynb_checkpoints

# ── OS generated files ──
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# ── Excel lock files ──
~$*.xlsx

# ── Runtime data (generated by pipeline, not source) ──
data/*.csv
data/*.xlsx
data/*.db

# ── LaTeX build artifacts ──
*.aux
*.log
*.synctex.gz
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.out
*.toc
*.lof
*.lot

# ── Temporary / scratch files ──
*.tmp
temp_*

# ── Paper drafts (managed in separate submission repo) ──
paper_draft.md
paper_struct.md

# ── Archive (legacy backups) ──
.archive/
